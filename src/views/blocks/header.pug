header(class=isDashboard ? 'dashboard-header' : '')
    div
        span.logo
            a(href='/') Do it!
        div
            if isUserLoggedIn
                a(href='/dashboard') Dashboard
                a(href='/logout') Logout
            else
                a(href='/login') Login
                a(href='/register') Create an account
    if isDashboard
        div.dashboard-menu
            a(href='/dashboard/todo-list') Todo list
            a(href='/dashboard/categories') Categories
            a(href='/dashboard/teams') Teams
